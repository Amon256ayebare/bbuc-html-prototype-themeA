# BBUC WiFi HTML Prototype (Theme A)

This is a fully connected static HTML prototype (no backend) for the WiFi Management System proof-of-concept.

## How to run
- Extract the folder and open `index.html` in any modern browser.
- Create an admin account via Register, then login.
- Data is stored in browser `localStorage`.
- Suitable to host on GitHub Pages.
